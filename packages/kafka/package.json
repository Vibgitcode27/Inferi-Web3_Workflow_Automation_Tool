{
  "name": "kafka",
  "version": "1.0.0",
  "description": "Kafka acts like a pub-sub here. Get events from the outbox and send them to processors.",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup",
    "test": "echo \"Error: no test specified\" && exit 1",
    "kafka:init": "ts-node src/init.ts"
  },
  "keywords": [
    "kafka"
  ],
  "author": "Vibgitcode27",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsup": "^8.4.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "kafkajs": "^2.2.4"
  }
}
